---
id: hiecm.error.abdm-1063
type: error
gateway: hiecm
milestone: M2
version: abdm-v3
title: ABDM-1063, HIP id mismatch in linking, invalid date range in consent
summary: >
  In linking, the HIP id does not match the link token. In the consent
  and data flow, the requested date range is invalid.
sources:
  - file: site/docs/api/hie-cm/m2/errors.md
    fetched: 2026-08-24
    hash: sha256:a22429e14d9f00a77445b833c16e34bf4249ca82e2316ea79709f609d2ddda93
verified:
  status: unverified
  against: docs-only
related:
  flows: [hiecm.flow.m2-link-care-context]
skills:
  - hiecm-m2-debug
  - hiecm-m3-debug
  - abdm-errors
---

## In plain words

Like its neighbour ABDM-1062, this code means different things in
different flows. In the M2 linking flow it means the HIP id in your
request is not the one the link token was issued for. In the consent and
data flow it means the date range you asked for is invalid. Read the
message text to know which case you are in.

## Before you start

Know which facility identity your request is running as, and in the
consent case, know the date range the consent artefact actually covers.

## What happens

Conditions that produce this error: in linking, using a link token that
was issued for a different HIP id, which happens when one system serves
several facilities and mixes their identities; in consent, requesting
records outside the granted range, or a range whose end precedes its
start.

## How you know it worked

Linking case: the link call succeeds, its exit condition per
hiecm.flow.m2-link-care-context observed, once the HIP id matches the
token. Consent case: the data request succeeds with a range inside what
the consent artefact grants.

## When it goes wrong

Linking fix: fetch the link token under the same HIP id that will send
the link request; audit which facility identity each step runs as.
Consent fix: read the consent artefact's permitted date range and
request inside it.
