---
id: hiecm.error.his-1124
type: error
gateway: hiecm
milestone: M4
version: abdm-v3
title: HIS-1124, the bridge is not linked to this facility
summary: >
  The call needs a bridge linked to the facility, and no such link
  exists.
sources:
  - file: catalogue/openapi/hiecm/v3/hiecm-m4.yaml
    fetched: 2026-09-04
    hash: sha256:626bfe09e359114b32b6cc5552223527a6e997a30687a5370a717ef49dd6cde4
    note: >
      The code and its message are from the M4 file's recorded error
      block. What to do about it is read from the flow it interrupts, not
      from a response observed against the sandbox.
verified:
  status: unverified
  against: docs-only
related:
  flows: [hiecm.flow.m4-link-bridge]
skills:
  - hiecm-m4-debug
  - abdm-errors
---

## In plain words

A facility ID and a bridge id are two separate registrations. Until they
are linked, the bridge cannot act for the facility, and a call that
assumes it can is refused.

## Before you start

Know the facility's facility ID and the bridge id of the software acting
for it, and know which role that software needs, provider or requester.

## What happens

Conditions that produce this error: sharing or fetching attempted before
the bridge was linked, a link created for the other role, or a link whose
`active` is false.

## How you know it worked

The bridge linkage call reports the link present and active, and the
call that failed then succeeds.

## When it goes wrong

Link the bridge, and check the `type`. A facility whose software both
publishes and requests records needs one link of each type, not one link
that claims both.
