---
id: hiecm.error.abdm-2404
type: error
gateway: hiecm
milestone: M2
version: abdm-v3
title: ABDM-2404, your REQUEST-ID is missing, malformed or reused
summary: >
  ABDM-2404 is NHA's code for invalid request id. The `REQUEST-ID`
  header is not a UUID the gateway will accept.
sources:
  - file: ABDM Sandbox/ABDM/Proposed Simplified Milestone 2.docx
    status: not-yet-hashed
    note: >
      NHA's error code table, which supplies the code and NHA's own
      wording.
verified:
  status: unverified
related:
  glossary: [shared.glossary.request-id]
  concepts: [hiecm.concept.asynchronous-callbacks]
fix:
  deterministic: true
---

# ABDM-2404, your REQUEST-ID is missing, malformed or reused

## In plain words

The `REQUEST-ID` header is not a UUID the gateway will accept.

It matters more than it looks. In the asynchronous flows this value is
how the callback that answers your call is matched to it, so a bad one
breaks correlation as well as this request.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Invalid Request Id".


  Three conditions produce it: the header is absent, the value is not a
  UUID, or the value has already been used.

  Reuse is the one people do not expect. A hardcoded UUID left over from
  testing works the first time and fails afterwards.

## How you know it worked

The call returns its normal response, and in an asynchronous flow the callback arrives carrying the same `REQUEST-ID` you sent.

## When it goes wrong

Generate a fresh UUID per request and log it before sending.

If you are reusing a value deliberately to retry, generate a new one
instead. Retrying is not the same as repeating.

