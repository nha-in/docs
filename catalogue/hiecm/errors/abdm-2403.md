---
id: hiecm.error.abdm-2403
type: error
gateway: hiecm
milestone: M2
version: abdm-v3
title: ABDM-2403, you are pointed at the wrong consent manager
summary: >
  ABDM-2403 is NHA's code for invalid x-cm-id. The `X-CM-ID` header
  does not name a consent manager the gateway
sources:
  - file: ABDM Sandbox/ABDM/Proposed Simplified Milestone 2.docx
    status: not-yet-hashed
    note: >
      NHA's error code table, which supplies the code and NHA's own
      wording.
verified:
  status: unverified
related:
  glossary: [shared.glossary.x-cm-id]
fix:
  deterministic: true
---

# ABDM-2403, you are pointed at the wrong consent manager

## In plain words

The `X-CM-ID` header does not name a consent manager the gateway
recognises for this host.

In practice this nearly always means sandbox and production have been
mixed: the right host with the other environment's value.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Invalid X-CM-ID".


  The value is `sbx` on the sandbox and `abdm` in production. Sending
  either against the wrong host produces this code, as does omitting the
  header on a call that needs it.

  NHA giving this its own code, rather than folding it into a generic
  header error, tells you how often it happens.

## How you know it worked

The call returns its normal response. Confirm by checking that your configured host and your `X-CM-ID` come from the same environment configuration, not from two places.

## When it goes wrong

Set `X-CM-ID: sbx` when the host is `dev.abdm.gov.in`, and
`X-CM-ID: abdm` when it is `apis.abdm.gov.in`.

The durable fix is to derive both from one environment setting, so they
cannot disagree.

