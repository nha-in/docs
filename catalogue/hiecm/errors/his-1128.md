---
id: hiecm.error.his-1128
type: error
gateway: hiecm
milestone: M4
version: abdm-v3
title: HIS-1128, that HIP name is already in use
summary: >
  The name you chose for this bridge is already taken, and the registry
  requires it to be unique.
sources:
  - file: catalogue/openapi/hiecm/v3/hiecm-m4.yaml
    fetched: 2026-09-04
    hash: sha256:626bfe09e359114b32b6cc5552223527a6e997a30687a5370a717ef49dd6cde4
    note: >
      The code and its message are from the M4 file's recorded error
      block. What to do about it is read from the flow it interrupts, not
      from a response observed against the sandbox.
related:
  flows: [hiecm.flow.m4-link-bridge]
skills:
  - hiecm-m4-debug
  - abdm-errors
---

## In plain words

The HIP name is what a patient sees in their own application when they
search for this hospital. It must be unique for every bridge on a
facility, so a second bridge cannot reuse the first one's name.

## Before you start

Have a naming rule before you link the second bridge. Fifteen characters
is not much room, and the rule is easier to apply than to invent twice.

## What happens

Conditions that produce this error: linking a second bridge under the
name the first one already holds, or a retry that reuses a name a partly
succeeded call already took.

## How you know it worked

The bridge linkage call is accepted with the new name, and the facility
carries two bridges with two distinct names.

## When it goes wrong

Choose a different name inside the three rules: 15 characters or fewer,
no special characters, unique on this facility. NHA's worked example
builds it from the hospital name plus the bridge name.
