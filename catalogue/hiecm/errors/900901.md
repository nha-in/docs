---
id: hiecm.error.900901
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: 900901, the ABHA service rejected the credentials outright
summary: >
  900901 is NHA's code for invalid credentials. A second ABHA service
  code observed in NHA's saved responses, paired with
sources:
  - file: ABDM Sandbox/ABDM/M1 ABHA Collection.postman_collection.json
    status: not-yet-hashed
    note: >
      Observed in a response body saved in NHA's own M1 collection.
verified:
  status: unverified
related:
  concepts: [hiecm.concept.gateway-session]
---

# 900901, the ABHA service rejected the credentials outright

## In plain words

A second ABHA service code observed in NHA's saved responses, paired with
the message "Invalid Credentials".

Unlike the unclassified failure, this one is definite: what you sent was
wrong, not merely unacceptable.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Invalid Credentials".


  Observed in NHA's M1 collection. It appears where a token or a client
  credential was wrong rather than expired.

  Because it is a credential judgement rather than a token lifetime one,
  refreshing will not help.

## How you know it worked

The call returns its normal response. Verify by making the session call fresh and using its token immediately.

## When it goes wrong

Re-check the client id and secret against the ones issued for this
environment, then get a fresh session token.

Sandbox credentials do not work against production. That is the most
common cause of a definite rather than an expired credential failure.

