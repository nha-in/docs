---
id: hiecm.error.abdm-2401
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: ABDM-2401, the person scoped token is wrong or expired
summary: >
  ABDM-2401 is NHA's code for the x auth token is invalid. This is not
  about your application's session token. It is about the token
sources:
  - file: ABDM Sandbox/ABDM/Proposed Simplified Milestone 2.docx
    status: not-yet-hashed
    note: >
      NHA's error code table, which supplies the code and NHA's own
      wording.
verified:
  status: unverified
related:
  concepts: [hiecm.concept.gateway-session]
---

# ABDM-2401, the person scoped token is wrong or expired

## In plain words

This is not about your application's session token. It is about the token
that identifies one person, returned when they logged in and sent
afterwards as `X-token`.

The gateway accepted your application and rejected the person.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "The X Auth token is invalid".


  The token has expired, belongs to a different person than the account
  being read, or was never obtained because the login flow was not
  completed.

  In M1 the profile calls are the ones that need it.

## How you know it worked

The profile call returns the account you expected, and the identifiers in the response match the person who logged in.

## When it goes wrong

Have the person log in again, or refresh the token using the refresh
token rather than making them repeat the OTP.

If it persists, check you are not sending the application session token
in `X-token`. The two tokens are not interchangeable.

