---
id: hiecm.error.abdm-1107
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: ABDM-1107, invalid combinations of scopes
summary: >
  The scope array on this call does not match what the operation expects, or
  does not match the scope the transaction was opened with. The message names
  the field and not which of the two calls disagreed.
sources:
  - file: catalogue/openapi/hiecm/v3/hiecm-m1.yaml
    status: not-yet-hashed
    note: >
      The scope examples for the login and enrolment operations, and the
      recorded code.
  - file: (private) sandbox session, abhasbx.abdm.gov.in, 2026-09-14
    status: not-publicly-citable
    role: corroboration
    note: >
      Observed on an enrolment verification that reused the request's scope.
related:
  endpoints: [hiecm.endpoint.m1-login-request-otp, hiecm.endpoint.m1-login-verify, hiecm.endpoint.m1-enrolment-request-otp, hiecm.endpoint.m1-enrolment-verify-abdm-otp]
  concepts: [hiecm.concept.m1-journey-design]
skills:
  - hiecm-m1-debug
---

# ABDM-1107, invalid combinations of scopes

## In plain words

`scope` is an array, and the operations care about the combination rather than
about any one value in it. This code means the combination you sent is not one
this operation accepts, or is not the one the transaction was opened with.

```response
{"error": {"code": "ABDM-1107", "message": "Invalid combinations of scopes"}}
```

Observed on the sandbox on 2026-09-14: an enrolment OTP request sent
`["abha-enrol"]` and returned 200, and the verification that followed reused
`["abha-enrol"]` and was refused with this code.

## Before you start

- The request that opened the transaction, and the scope it carried.
- The operation you are calling now, and the scope its own examples carry. The
  two are not always the same array.

## What happens

Two rules, and the second is the one that surprises people.

**A login keeps one pair across both calls.** The pair is `abha-login` plus the
thing that verifies the person, and the same pair goes on the OTP request and
on the verification:

| Logging in with | scope |
|---|---|
| A mobile OTP | `["abha-login", "mobile-verify"]` |
| An Aadhaar OTP | `["abha-login", "aadhaar-verify"]` |
| A password | `["abha-login", "password-verify"]` |

**An enrolment changes pair between the calls.** The OTP request carries
`["abha-enrol"]` alone, and the verification carries `["abha-enrol"]` plus what
is being verified, for example `["abha-enrol", "mobile-verify"]`. Carrying the
request's scope forward into the verification is what produces this code.

## How you know it worked

The call the refusal came from returns its own success body instead: a login
verification returns `authResult: "success"` with a token and an accounts
array, and an enrolment verification continues the enrolment.

## When it goes wrong

- You reused one scope across a whole journey. Read the scope off the example
  for the operation you are calling, not off the one before it.
- You are on the enrolment path and meant to be on the login path. Aadhaar is a
  login identifier as well as an enrolment one, and the scope is the first
  place the two diverge. A patient who already holds an ABHA belongs on the
  login path, and sending them down enrolment creates a second ABHA number that
  nothing merges. See [designing the ABHA journey](../concepts/m1-journey-design.md).
- The transaction was opened for something else. `txnId` carries the scope it
  was created with, so a transaction cannot be reused across journeys.
