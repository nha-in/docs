---
id: hiecm.error.abdm-1094
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: ABDM-1094, access to this feature is restricted
summary: >
  Your client is not entitled to the endpoint you called, or the
  BENEFIT_NAME header does not match a registered benefit programme.
sources:
  - file: site/docs/api/hie-cm/m1/errors.md
    fetched: 2026-08-24
    hash: sha256:7aa70d618346c2c231628d6e3e8e58e32429359468334b6dc946793173793895
verified:
  status: unverified
  against: docs-only
related:
  endpoints:
    - hiecm.endpoint.m1-benefit-get-by-abha
    - hiecm.endpoint.m1-benefit-find-abha-by-aadhaar
    - hiecm.endpoint.m1-benefit-find-aadhaar-by-abha
skills:
  - hiecm-m1-debug
  - abdm-errors
---

## In plain words

ABDM-1094 is an entitlement failure, HTTP 401. Your client id is not
allowed to do what it just tried. It carries two documented messages:
"Access to this feature is restricted. Please contact NHA to enable it"
means the endpoint itself is off limits for your client; "Invalid
Benefit Name" means the BENEFIT_NAME header you sent does not match a
benefit programme registered against your client.

## Before you start

Know which integrator category you are. Every saved example of the
restricted message in NHA's M1 collection is on a benefit programme or
insurance call, which are government integrator only.

## What happens

Conditions that produce this error: calling a benefit or insurance
endpoint without the entitlement, or sending a BENEFIT_NAME value NHA
did not register for you. The body shape varies: the same code appears
both wrapped in an `error` object and as a top level `code` field with a
timestamp. Read `error.code` first and fall back to the top level
`code`. One saved example shows the code reported as ABDM-9999 with the
real ABDM-1094 message inside the message text, so log the raw body.

## How you know it worked

The original call returns a non 401 response. For the benefit name case,
NHA's own collection uses the exact registered name, such as
`healthid api`.

## When it goes wrong

Retrying does not help; entitlement is configuration, not a transient
state. Restricted message: stop calling the endpoint, or ask NHA to
enable the entitlement. Invalid benefit name: use the exact name NHA
gave you at onboarding.
