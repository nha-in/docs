---
id: hiecm.error.his-2045
type: error
gateway: hiecm
milestone: M4
version: abdm-v3
title: HIS-2045, the session behind your transaction id has expired
summary: >
  The transaction you started has timed out, so the transaction id you are
  still sending no longer stands for anything.
sources:
  - file: catalogue/openapi/hiecm/v3/hiecm-m4.yaml
    fetched: 2026-09-04
    hash: sha256:626bfe09e359114b32b6cc5552223527a6e997a30687a5370a717ef49dd6cde4
    note: >
      The code and its message are from the M4 file's recorded error
      block. What to do about it is read from the flow it interrupts, not
      from a response observed against the sandbox.
verified:
  status: unverified
  against: docs-only
related:
  flows: [hiecm.flow.m4-create-hpid]
skills:
  - hiecm-m4-debug
  - abdm-errors
---

## In plain words

Aadhaar authentication runs as a session on the registry's side, keyed to
the `txnId` you were given. That session has ended. The id is not wrong,
it is finished.

## Before you start

Know when your `txnId` was issued. The URL the professional is sent to is
valid for five minutes, and the session behind it does not outlive the
work it was opened for.

## What happens

Conditions that produce this error: a professional who left the Aadhaar
page open and came back, a retry of a later step long after the earlier
one succeeded, or a `txnId` cached from an abandoned attempt.

## How you know it worked

Generate the Aadhaar link again, redirect with the fresh URL, and the
step that failed is accepted with the new `txnId`.

## When it goes wrong

Do not retry with the same `txnId`. It cannot be revived, and a retry
loop on it will spend its attempts against a session that has closed.
Start the flow again from generate Aadhaar link.
