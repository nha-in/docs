---
id: hiecm.error.abdm-1016
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: ABDM-1016, your timestamp format is wrong
summary: >
  ABDM-1016 is NHA's code for an invalid `TIMESTAMP` header. The observed
  cause is the wrong format, not a wrong clock, and the sandbox wraps it
  in a misleading HTTP 404.
sources:
  - url: https://abhasbx.abdm.gov.in
    fetched: 2026-08-25
    status: not-yet-hashed
    note: >
      Observed session against the ABHA sandbox service on 2026-08-25.
      One request was rejected with this code and an otherwise identical
      request with a reformatted TIMESTAMP succeeded. There is no NHA
      document to hash; the source is the recorded session itself.
verified:
  status: verified
  against: https://abhasbx.abdm.gov.in (ABHA sandbox)
  on: "2026-08-25"
  by: recorded integration session against the ABHA sandbox, request and response pairs observed
related:
  errors: [hiecm.error.abdm-2402]
  glossary: [shared.glossary.timestamp-header]
fix:
  deterministic: true
---

# ABDM-1016, your timestamp format is wrong

## In plain words

Your `TIMESTAMP` header is in a format the service does not accept, so
the request was refused before anything else was looked at.

This is a formatting problem, not a clock problem. Its sibling,
[ABDM-2402](./abdm-2402.md), is the clock drift case: the value is well
formed but too far from the gateway's own time. If your value parses as
ISO 8601 UTC with milliseconds and still fails, read that atom instead.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Invalid TimeStamp".

We observed it on 2026-08-25 against `https://abhasbx.abdm.gov.in`, the
ABHA sandbox service. A request with the header

```
TIMESTAMP: 2026-08-25T21:12:40.588+05:30
```

was rejected. The identical request with

```
TIMESTAMP: 2026-08-25T15:51:15.339Z
```

succeeded. The rejected value was ISO 8601 with the IST offset; the
accepted value was ISO 8601 UTC with milliseconds and the `Z` suffix.
So the observed trigger is format: an offset form, or a value missing
milliseconds, rather than a drifted clock.

Two things about the rejection will mislead you.

First, the HTTP status was 404, not 400, even though nothing was
missing. The status code lied; the body code was the truth. Match on
the body, not the status. A rejected header looks like this:

```json
{"error":{"code":"ABDM-1016: ","message":"Invalid Timestamp"}}
```

A genuinely wrong path returns a 404 with a completely different body
shape:

```json
{"code":"404","type":"Status report","message":"Not Found"}
```

Second, the `code` field itself carried a trailing colon and space, the
literal string `ABDM-1016: `. A naive exact-match parser looking for
`ABDM-1016` will miss it. Trim the field, or match on the prefix.

Note a tension between services. [ABDM-2402](./abdm-2402.md) records
that working HIE-CM integrations send IST with the +05:30 offset. This
observation shows the ABHA sandbox service rejecting exactly that form
and accepting UTC. We have not reconciled the two; treat the accepted
format as per service until someone verifies both against the same
build.

## How you know it worked

The call you were making returns its normal response rather than this
code. In the observed session, resending the identical request with the
`TIMESTAMP` reformatted to UTC succeeded immediately, so no wait or
retry backoff is involved.

## When it goes wrong

Format the header as ISO 8601 UTC with milliseconds and the `Z` suffix,
for example `2026-08-25T15:51:15.339Z`. That exact change is what turned
the observed failure into a success. Generate it from your language's
date library, for example `Instant.now().toString()` in Java or
`new Date().toISOString()` in JavaScript. Do not format the value by
hand.

If a well formed UTC value still fails, your clock has probably
drifted, which is [ABDM-2402](./abdm-2402.md)'s territory.

If you are matching error codes and never see `ABDM-1016`, check that
your parser is not doing an exact match on the `code` field. The
observed value carried a trailing colon and space.
