---
id: hiecm.error.900900
type: error
gateway: hiecm
milestone: M1
version: abdm-v3
title: 900900, the ABHA service rejected the call without saying why
summary: >
  900900 is NHA's code for unclassified authentication failure. A code
  from the ABHA service rather than from the gateway, observed in
sources:
  - file: ABDM Sandbox/ABDM/M1 ABHA Collection.postman_collection.json
    status: not-yet-hashed
    note: >
      Observed in a response body saved in NHA's own M1 collection.
verified:
  status: unverified
related:
  concepts: [hiecm.concept.gateway-session]
---

# 900900, the ABHA service rejected the call without saying why

## In plain words

A code from the ABHA service rather than from the gateway, observed in
NHA's own saved responses. The accompanying description names the API
path that was refused.

It means authentication failed and the service is not saying which part.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Unclassified Authentication Failure".


  Observed in NHA's M1 collection against profile routes. The saved
  response pairs the code with a message of "Unclassified Authentication
  Failure" and a description naming the path.

  M1 has two tokens, the application session token and the person scoped
  `X-token`, so either can be the cause.

## How you know it worked

The call returns the account or result you expected rather than an error envelope.

## When it goes wrong

Check both tokens. Confirm `Authorization` carries a current session
token, and that `X-token` carries the token for the person whose account
you are reading.

If both look right, confirm the route is one your credentials are
entitled to call. Gateway subscription state produces refusals that look
like credential problems.

