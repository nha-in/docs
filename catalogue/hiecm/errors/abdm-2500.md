---
id: hiecm.error.abdm-2500
type: error
gateway: hiecm
milestone: M2
version: abdm-v3
title: ABDM-2500, you did not send a session token
summary: >
  ABDM-2500 is NHA's code for authorization header is missing. The
  call arrived with no `Authorization` header, so the gateway does not
sources:
  - file: ABDM Sandbox/ABDM/Proposed Simplified Milestone 2.docx
    status: not-yet-hashed
    note: >
      NHA's error code table, which supplies the code and NHA's own
      wording.
verified:
  status: unverified
related:
  concepts: [hiecm.concept.gateway-session]
  endpoints: [hiecm.endpoint.gateway-sessions]
fix:
  deterministic: true
---

# ABDM-2500, you did not send a session token

## In plain words

The call arrived with no `Authorization` header, so the gateway does not
know which application is calling.

Every ABDM call except the session call itself needs this.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Authorization header is missing".


  Either the header was never set, or a token refresh failed silently and
  your client sent an empty value.

  It also appears when the header is present but shaped wrongly. The value
  is the word `Bearer`, a space, then the access token.

## How you know it worked

The call returns its normal response. A stronger check is to confirm your client sends the header on every route, not only on the ones you tested.

## When it goes wrong

Get an access token from the session endpoint and send it as
`Authorization: Bearer <ACCESS_TOKEN>`.

If tokens expire mid-session, refresh on `expiresIn` rather than waiting
for a failure, since a failed refresh presents as this code rather than
as a refresh error.

