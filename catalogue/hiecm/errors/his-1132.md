---
id: hiecm.error.his-1132
type: error
gateway: hiecm
milestone: M4
version: abdm-v3
title: HIS-1132, the registry detected a duplicate facility
summary: >
  What you sent looks like a facility the registry already holds, so the
  write was refused.
sources:
  - file: catalogue/openapi/hiecm/v3/hiecm-m4.yaml
    fetched: 2026-09-04
    hash: sha256:626bfe09e359114b32b6cc5552223527a6e997a30687a5370a717ef49dd6cde4
    note: >
      The code and its message are from the M4 file's recorded error
      block. What to do about it is read from the flow it interrupts, not
      from a response observed against the sandbox.
related:
  flows: [hiecm.flow.m4-onboard-facility]
skills:
  - hiecm-m4-debug
  - abdm-errors
---

## In plain words

The registry runs its own duplicate detection on the values you send,
independently of whether you ran the deduplicate search first.

## Before you start

Run the deduplicate search first, and read its result rather than
treating an empty screen as an empty registry.

## What happens

Conditions that produce this error: a facility whose name and location
match an existing record closely enough, or a second onboarding attempt
for a facility already in draft from an earlier run.

## How you know it worked

The deduplicate search returns the matching facility, and your system
continues with that facility rather than creating one.

## When it goes wrong

Search, then decide. Where the match is genuinely a different facility
at the same address, the distinguishing detail belongs in the name and
address fields before you write again.
