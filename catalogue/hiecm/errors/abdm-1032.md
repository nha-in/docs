---
id: hiecm.error.abdm-1032
type: error
gateway: hiecm
milestone: M2
version: abdm-v3
title: ABDM-1032, one of the required headers is absent or malformed
summary: >
  ABDM-1032 is NHA's code for invalid header. A required header is
  missing or unusable, and NHA is not saying which
sources:
  - file: ABDM Sandbox/ABDM/Proposed Simplified Milestone 2.docx
    status: not-yet-hashed
    note: >
      NHA's error code table, which supplies the code and NHA's own
      wording.
verified:
  status: unverified
related:
  errors: [hiecm.error.abdm-2402, hiecm.error.abdm-2403, hiecm.error.abdm-2404, hiecm.error.abdm-2500]
---

# ABDM-1032, one of the required headers is absent or malformed

## In plain words

A required header is missing or unusable, and NHA is not saying which
one.

This is the code you get when the more specific header codes do not
apply.

## Before you start

You have received this code in a response body. Nothing else is assumed.

## What happens

NHA's own wording for this code is "Invalid header".


  The headers ABDM requires across the gateway are `REQUEST-ID`,
  `TIMESTAMP`, `X-CM-ID` and `Authorization`. M2 and M3 calls add per flow
  tokens.

  Because the code is generic, work through them in that order rather than
  guessing.

## How you know it worked

The call returns its normal response. Log the full header set you sent on the failing request, so the next occurrence is a comparison rather than a hunt.

## When it goes wrong

Check each required header is present, spelled exactly as NHA spells it,
including the hyphens and the case, and carries a well formed value.

NHA also publishes more specific codes: `ABDM-2402` for the timestamp,
`ABDM-2403` for the consent manager id, `ABDM-2404` for the request id
and `ABDM-2500` for a missing authorisation header. Receiving the generic
code means none of those matched.

