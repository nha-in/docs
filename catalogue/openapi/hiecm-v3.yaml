openapi: 3.0.3
info:
  title: HIE-CM (ABDM V3)
  version: abdm-v3
  description: >
    Placeholder specification for NHA's HIE-CM V3 gateway. This file will be
    replaced by the real specification ingested from
    https://sandbox.abdm.gov.in swagger sources following the openapi-ingest
    process: fetch raw to .raw/, hash, lint, record corrections in
    corrections/, then update this cleaned copy.
servers:
  - url: https://dev.abdm.gov.in
    description: ABDM sandbox gateway
  - url: https://abdm.gov.in
    description: ABDM production gateway
tags:
  - name: placeholder
    description: Remove when the real specification is ingested.
paths:
  /health:
    get:
      operationId: healthCheck
      tags: [placeholder]
      summary: Placeholder operation so the reference renders
      description: >
        This operation exists only so the Scalar reference page renders while
        the real HIE-CM V3 specification is being ingested. It is not an NHA
        endpoint.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: placeholder
