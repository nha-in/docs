# NHCX session, from the NHCX package's Bruno folder apis/01-session.
#
# Written by the package's system/build-ekadocs.mjs (make ekadocs); change the .bru
# requests there and port again rather than editing this file.
#
# Rules this file follows: catalogue/openapi/CONVENTIONS.md
openapi: 3.1.1
info:
  x-portal:
    module: session
    label: Session
    position: 1
  title: NHCX session
  summary: The token does not come from NHCX.
  description: |-
    The Session calls on the National Health Claims Exchange (NHCX): 1 operation.

    The token does not come from NHCX.

    Guides that use these calls: [Session token](/docs/nhcx/v1/getting-started/session-token).
  version: nhcx-v1
  license:
    name: MIT
    identifier: MIT
  contact:
    name: NHCX sandbox
    url: https://hcxsbx.abdm.gov.in
  x-abdm-gateway: nhcx
  x-abdm-module: session
  x-abdm-phase: 1
  x-abdm-roles:
    - provider
    - payer
x-abdm-sources:
  - file: nhcx-package/apis/01-session
    role: upstream
    hash: sha256:04dbfe1121fdee8812399abb612d00cac445a312b9a88dc30881f34ce2b1059b
    note: The package's Bruno collection, one request per operation.
  - file: nhcx-package/baseurl.yaml
    role: upstream
    hash: sha256:47d8412abbfc53b1e1038410e7980eee029d7b1be5082f4ae6c1d4e16ae0dd0f
    note: The sandbox and production hosts.
servers:
  - url: https://dev.abdm.gov.in
    description: Sandbox, ABDM session token.
  - url: https://apis.abdm.gov.in
    description: Production. ABDM's published production gateway. Confirm it in your onboarding letter.
tags:
  - name: Session
    description: The token does not come from NHCX.
security:
  - bearerAuth: []
paths:
  /api/hiecm/gateway/v3/sessions:
    post:
      operationId: session_session_token
      x-abdm-use-case: Session
      tags:
        - Session
      summary: Session token
      description: |-
        Mints the ABDM gateway session token that every NHCX call carries, from the client ID and secret issued for Milestone 1.

        ### Business purpose

        The token does not come from NHCX. It comes from the ABDM gateway, and one token serves every call: the participant service, the use-case endpoints and the status check. Nothing else in this collection works without it, which is why the post-response script below stores it in `token`, so that running this request once arms the rest of the collection.

        ### When to use

        Before the first call, and again whenever the token you hold is more than a few minutes old or any call answers `401`. Build against this v3 address.

        ### Preconditions

        - Milestone 1 is complete and you hold its client ID and secret.
        - `REQUEST-ID` is a fresh UUID generated for this call.
        - `TIMESTAMP` is the current time in UTC, ISO 8601 with milliseconds and a trailing `Z`, as in `2026-09-04T06:15:51.975Z`, taken from a synchronised system clock.
        - `X-CM-ID` is `sbx` on the sandbox.
        - The body carries `grantType` `client_credentials`, which the v3 address requires.

        ### Postconditions

        The gateway returns `accessToken`, `expiresIn`, `refreshToken` and `tokenType` `bearer`. On every NHCX call the token goes in `bearer_auth` as `Bearer <token>`. Some pages and the notification endpoint use `Authorization` instead, so this collection sends both with the same value. The documents put the token's life at 300, 1200 and 6000 seconds in different places, so do not rely on any of them.

        ### Common mistakes

        - Reusing a `REQUEST-ID` copied from an example instead of generating a new one.
        - A `TIMESTAMP` built by hand or read from a drifted clock, which is refused.
        - Omitting `grantType`, as older samples do, which the v3 address rejects.
        - Sending the token without the `Bearer ` prefix, the portal's own example of a `401`.
        - Retrying a `401` with the same token. It fails the same way.
        - Writing the token or the client secret to a log.

        ### Best practices

        - Keep the token together with the time you got it, and fetch a new one before a call if it is more than a few minutes old.
        - On any `401`, get a new token and retry that call once.
        - Send the token on both `bearer_auth` and `Authorization`.
        - Keep the clock synchronised with NTP and let a date library format the timestamp.

        ### Related scenario

        A hospital's integration starts its first eligibility check of the day. It posts this request with a new `REQUEST-ID`, the current UTC `TIMESTAMP` and `X-CM-ID` `sbx`, stores the `accessToken` with the time it arrived, and sends it on the check as `bearer_auth: Bearer <token>`. Later a claim call answers `401 Sender is not authorized to execute the operation`. The integration mints a fresh token, retries the claim once, and it goes through.

        ### Specification

        Chapter [Base URLs](/docs/nhcx/v1/getting-started/base-urls) of the NHCX integration specification.
      security: []
      parameters:
        - $ref: "#/components/parameters/RequestId"
        - $ref: "#/components/parameters/Timestamp"
        - $ref: "#/components/parameters/XCmId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                clientId:
                  type: string
                clientSecret:
                  type: string
                grantType:
                  type: string
            example:
              clientId: <client id>
              clientSecret: <client secret>
              grantType: client_credentials
      responses:
        "200":
          description: The gateway returns `accessToken`, `expiresIn`, `refreshToken` and `tokenType` `bearer`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken:
                    type: string
                  expiresIn:
                    type: integer
                  refreshTokenIn:
                    type: integer
                  refreshToken:
                    type: string
                  tokenType:
                    type: string
              example:
                accessToken: eyJhbGciOiJSUzI1NiIs...
                expiresIn: 300
                refreshTokenIn: 300
                refreshToken: eyJhbGciOiJSUzI1NiIs...
                tokenType: bearer
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: "On every NHCX call, the token goes in a header called `bearer_auth`, with the word `Bearer` and a space in front. The sources are not unanimous: the authentication page and the FAQ both write the example as `Authorization`, and the notification endpoint uses `Authorization`. The safe course, and what the adapter does, is to send both headers with the same value."
  parameters:
    RequestId:
      name: REQUEST-ID
      in: header
      required: true
      description: "`REQUEST-ID` is a fresh UUID that you generate for every call. Sending the same one twice is the mistake to avoid; generate it, do not copy it from an example."
      schema:
        type: string
      example: <uuid>
    Timestamp:
      name: TIMESTAMP
      in: header
      required: true
      description: "`TIMESTAMP` is the current time in UTC, ISO 8601 with milliseconds and a trailing `Z`, as in `2026-09-04T06:15:51.975Z`. A clock that has drifted will be refused, so take the time from the system rather than constructing it by hand. How to produce it in each language is at the end of this chapter."
      schema:
        type: string
      example: <iso timestamp>
    XCmId:
      name: X-CM-ID
      in: header
      required: true
      description: "`X-CM-ID` names the environment. It is `sbx` on the sandbox. The mirror and the adapter both use lowercase."
      schema:
        type: string
      example: sbx
