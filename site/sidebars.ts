import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

// One sidebar per top-level tab. Each is generated from its folder, so a page
// lands where its frontmatter and folder say it lands, never where a hand
// edit here says it does.
const sidebars: SidebarsConfig = {
  overviewSidebar: [{type: 'autogenerated', dirName: 'overview'}],
  apiSidebar: [{type: 'autogenerated', dirName: 'api'}],
  whatsNewSidebar: [{type: 'autogenerated', dirName: 'whats-new'}],
  supportSidebar: [{type: 'autogenerated', dirName: 'support'}],
};

export default sidebars;
