---
title: Sequence
sidebar_label: Sequence
description: Every step of every M1 use case, what it should do, and the call it makes.
hide_table_of_contents: true
wrapperClassName: api-doc
verification: unverified
source: NHA's milestone document and the published specifications
---

import {NextStep} from '@site/src/components/docs/PathForward';

import TestMatrix from '@site/src/components/api/TestMatrix';
import matrix from '@site/src/data/test-matrix/m1.json';

# Sequence

Pick a use case to see its steps in order. Each step says what it should do,
what you should see when it works, and which call it makes. Search to jump to a
step or an endpoint.

<TestMatrix matrix={matrix} />

<NextStep
  label="When it goes wrong"
  detail="The four error shapes NHA actually returns, the recorded codes, and the fix for each."
  to="/docs/abdm/v3/api/m1/errors"
/>
