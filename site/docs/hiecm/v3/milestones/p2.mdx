---
title: P2 Linking and records
sidebar_label: P2 Linking
sidebar_position: 10
sidebar_custom_props:
  roles: [phr]
description: 'The patient side of M2: how a PHR app finds records held elsewhere, links them to an ABHA address, and shares a profile at a facility.'
verification: unverified
source: catalogue/openapi/hiecm/v3/hiecm-p2.yaml
sidebar_class_name: sidebar-icon sidebar-icon--link-2
---

import ApiLinks from '@site/src/components/docs/ApiLinks';

# P2 Linking and records

P2 is the mirror of [M2 Attach](./m2). M2 is a provider publishing a record. P2
is the patient discovering it and linking it to their own [ABHA
address](/docs/hiecm/v3/getting-started/glossary#abha-address).

<ApiLinks module="p2" />

## In short

- Discovery is for facilities the user visited without giving an ABHA address,
  and for old records.

## What you build

Care context linking, user initiated linking, scan and share at a facility, and
health locker subscriptions.

## Scan and share at a facility

The share request carries two facility values: the HIP ID and a counter ID,
the `context`. Your app:

1. Calls the [HIE-CM](/docs/hiecm/v3/getting-started/glossary#hie-cm) to share
   the details.
2. Waits for the facility's response.
3. Displays the token number if the facility returned one.

A counter ID is 1 to 250 characters: letters, digits and spaces, with `.`, `-`
and `_` allowed between them.

## Discovery and user initiated linking

The user picks the facility. Your app sends a discovery request carrying the
HIP ID and unverified identifiers of type `MR`, `MOBILE`, `ABHA_NUMBER` or
`ABHA_ADDRESS`.

The user selects care contexts and confirms. The HIP sends an
[OTP](/docs/hiecm/v3/getting-started/glossary#otp) to the registered mobile
number, and on successful verification the care contexts link to the ABHA
address.

## Where the citizen is the HIP

A citizen pushing a record into your app is the HIP. A health locker, where
users upload their own records, puts you on that publishing side.

An uploaded record is shareable once you hold three things: a link token from
the generate link token call, a care context added by HIP initiated linking from [M2
Attach](./m2), and the M2 health information transfer APIs.

## Next

- The calls and base URLs: [P2 API reference](/reference/hiecm-p2).
- The next milestone: [P3 Consent and notifications](./p3).
