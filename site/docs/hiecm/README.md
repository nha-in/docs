# HIE-CM

One folder per published version (v3 today). Creating a new version folder
adds it to the version picker; the newest folder becomes the default.
`_platform.json` holds the picker label, description, order, and notes for
versions that exist but are not published here.

