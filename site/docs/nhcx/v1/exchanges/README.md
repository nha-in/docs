# Exchanges and codes

The endpoint catalogue and the workflow code tables, as prose, in the Docs
tab. The machine contracts for the same endpoints are generated into the API
references tab from `catalogue/openapi/nhcx/v1/`; nothing here duplicates a
schema.

Order pages with `sidebar_position` in the frontmatter.
