# Building a payer

The insurer, scheme or TPA side: the queues, the callbacks to host, the answers
to build, the screens each desk works from, and the sandbox exit checklist.

Order pages with `sidebar_position` in the frontmatter.
