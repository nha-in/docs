# Building a provider

The hospital side of the exchange, screen by screen and call by call. Generic
flow first (02 to 06), PMJAY differences in one page (07), then the UI guide,
the sandbox exit checklist, and the record of a live PMJAY sandbox run.

Order pages with `sidebar_position` in the frontmatter.
