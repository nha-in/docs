# Reset Handovers Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Close what the final-set reset handed over: PHR back in milestones, a site that builds with `onBrokenLinks: 'throw'`, an MCP index that builds and decodes errors from the new specs, an eval corpus whose provenance points at sources that exist, committed skill files that do not change with the environment, and a contributor plan that describes the repository as it is.

**Architecture:** Same branch and pipeline as `docs/superpowers/plans/2026-09-16-final-nha-swagger-reset.md`. The ingest mapping gains four PHR modules. The Go indexer reads specs by the same `<platform>/<version>/*.yaml` rule as `scripts/specs.mjs` and extracts error codes from response examples exactly as `scripts/lib/spec-errors.mjs` does. Everything else is edits to existing files.

**Tech Stack:** Node 22 ESM scripts, Go 1.2x under `mcp/`, Docusaurus under `site/`.

**Spec:** `docs/superpowers/specs/2026-09-16-final-nha-swagger-reset-design.md` (still binding), plus the user's decisions of 2026-09-17: PHR splits into P1, P2, P3 and Locker following NHA's collection folders; every loose end in the second handover is fixed; hand-written pages may now be edited on this branch.

## Global Constraints

- Branch `feat/final-nha-swagger`, worktree `.claude/worktrees/final-nha-swagger`. New commits only; never rewrite anything at or before `8e40ab4a5`. Push after each task's review passes. Explicit paths; never `git add -A`.
- Commit trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. No em dash anywhere, in files or commits.
- Nothing new is sourced from a retired document. The only sources are `catalogue/openapi/.raw/nha-2026-09-16/` and, for M1 call order only, its `abha/M1 ABHA Collection.json`. NHA's public FAQ web pages remain valid sources for the Ask AI eval corpus only.
- Generated files are never hand-edited: fix the script and regenerate.
- Prose on site pages and atoms follows `plugins/abdm-contributors-assistant/skills/nha-voice/SKILL.md` and `writing-guide/SKILL.md`: written as ABDM to the integrator, no "NHA says", no "simply/just/please".
- Leave the untracked `catalogue/openapi/.raw/nha-2026-09-15-review/` alone.
- Checks every task must leave passing: `npm run -s lint:specs` (0 errors), `check:specs`, `lint:journeys`, `lint:atoms`, `lint:sources`, `check:plugins`, `check:icons`, `lint:content`, `lint:agent`, `validate:skills`, and `(cd mcp && go vet ./... && go test ./...)` once Task 2 lands.

---

### Task 1: PHR splits into P1, P2, P3 and P4 Locker

**Files:** `scripts/ingest-nha.mjs`, `catalogue/openapi/hiecm/v3/journeys/` (delete `phr.yaml`, scaffold four), generated specs, `scripts/build-skills.mjs`, `scripts/compile-skills.mjs`, `site/src/components/docs/ApiLinks.tsx`, `site/src/css/sidebar-icons.css` (generated by `build:icons`), `skills-src/`, `plugins/abdm-integrators-assistant/skills/`, `site/src/data/api-routes.json`, `catalogue/atom-routes.json`.

Module mapping, by NHA tag, replacing the `phr` module:

| Tag | Module |
|---|---|
| `ABHA enrolment via Aadhaar`, `P1 - Create ABHA Address Flow`, `P1 - Login via ABHA Address - Password`, `P1 - PHR Login`, `P1-Registration-login` | `p1` |
| `P2 - Link ABHA Number`, `P2 - Switch Profile`, `P2 -PHR Profile`, `abdm-hiecm-patient-share-phr`, `abdm-hip-initiated-linking-phr`, `abdm-user-initiated-linking-phr`, `consent-management-data-flow-phr` | `p2` |
| `subscription-phr`, except the three calls below | `p3` |
| `GET .../patients/lockers`, `GET .../patients/lockers/{lockerId}`, `GET .../patients/requests` (tag `subscription-phr`) and `POST .../setup-locker` (tag `health-locker`, today in `subscription`) | `p4` |
| `abdm-hiecm-scan-pay-phr` | `scan-and-pay` |
| `Gateway` (the PHR swagger's `PUT /api/hiecm/gateway/v3/bridge-service`) | `gateway` |

Expected counts, asserted by the script: gateway 11, m1 30, m2 21, m3 12, m4 100, p1 11, p2 32, p3 8, p4 4, subscription 6, scan-and-pay 18 (total 253).

Module presentation (`x-portal`): p1 `P1 Registration and login`, p2 `P2 Management`, p3 `P3 Subscription`, p4 `P4 Locker`, positions 6 to 9, then subscription 10 and scan-and-pay 11. Roles `['phr']`. Icons: p1 `user-round`, p2 `files`, p3 `bell` (the icons the retired P1 to P3 modules used), p4 `lock`. Titles and summaries name what NHA's folder holds, one sentence, no invented facts. The PHR swagger file stays a source of all four.

The operationId prefix follows the new module (`p1_...`). Moving calls into `gateway` and `scan-and-pay` changes their prefix too.

Journeys: delete `journeys/phr.yaml`, rerun `node scripts/ingest-nha.mjs --journeys` so p1 to p4 are scaffolded, and fix `journeys/scan-and-pay.yaml`, `journeys/subscription.yaml` and `journeys/gateway.yaml` by deleting and re-scaffolding them too (all three were scaffolded, never hand-written). Never touch `m1.yaml`.

Skills: in `scripts/build-skills.mjs` and `scripts/compile-skills.mjs` replace the `phr` module with p1 to p4, each `rules: [UNVERIFIED]` and a one-sentence scope naming NHA's folder contents. `ApiLinks.tsx`'s `Module` type and label map cover every module id the specs now carry.

- [ ] Change the mapping, counts and `x-portal` values in `scripts/ingest-nha.mjs`; run `node scripts/ingest-nha.mjs` and `--check`.
- [ ] Re-scaffold the six journey files named above; `npm run -s lint:journeys`.
- [ ] Update the two skills scripts and `ApiLinks.tsx`; run `node scripts/build-api-reference.mjs && node scripts/compile-skills.mjs && node scripts/build-skills.mjs && npm run -s build:icons && npm run -s validate:skills`.
- [ ] `grep -rn "hiecm-phr\b\|moduleId === 'phr'\|abdm-phr\b\|'phr'" scripts site/src catalogue/openapi/hiecm/v3` returns only the `x-abdm-roles` value `phr`.
- [ ] All Global Constraints checks pass. Commit `feat: PHR returns as four milestones, following NHA's collection folders` (scripts, specs, journeys, correction log, skills-src, plugin skills, ApiLinks.tsx, icons css, api-routes.json, atom-routes.json if changed).

### Task 2: The MCP indexer builds, and decodes errors from the specs

**Files:** `mcp/cmd/indexer/main.go`, `mcp/internal/catalogue/operations.go`, its tests and `testdata`, `mcp/internal/index/*_test.go`, `mcp/internal/server/fixtures_test.go`, `mcp/internal/server/servertest/servertest.go`, `mcp/internal/server/tools.go` if its output names an action column.

- [ ] The indexer parses a spec only when its path relative to the catalogue is `openapi/<platform>/<version>/<name>.yaml` (exactly four segments), matching `scripts/specs.mjs`. Journeys, corrections and `.raw` are never parsed as OpenAPI.
- [ ] `specErrorCodes` stops reading `x-abdm-errors`. It walks every `paths` and `webhooks` operation's 4xx and 5xx responses, reading `content.application/json.example` and every `examples.*.value`, and yields each object with a string `code` matching `^[A-Z]{2,5}-\d{3,5}$|^\d{3,6}$` and a string `message`, first occurrence wins, attributed to the module and to the returning operationId. Read the YAML directly (kin-openapi may not expose `webhooks`). `Action` is dropped from `SpecErrorCode` and from any tool output; no invented action text replaces it.
- [ ] Tests: replace the `x-abdm-errors` fixture with a spec fixture carrying a 400 example `{error: {code: ABDM-1013, message: Invalid ABHA Number}}` and a 2xx example that also holds a `code`/`message` pair, and assert only the first is extracted. Update every fixture constructing `SpecErrorCode`.
- [ ] Prove: `cd mcp && go vet ./... && go test ./...`; `go run ./cmd/indexer -catalogue ../catalogue -out /tmp/idx.db` exits 0; `sqlite3 /tmp/idx.db` shows the operation count equal to 253 and the spec error code count equal to the number of distinct codes `errorsFromSpec` finds across all eleven specs (compute it with a node one-liner and state both numbers).
- [ ] Commit `fix: the MCP index reads specs by path depth and decodes errors from response examples`.

### Task 3: The eval corpus cites sources that exist

**Files:** `catalogue/annexure/askai-sources.md` (recreate), `scripts/lint-annexure.mjs` (restore from `git show origin/main:scripts/lint-annexure.mjs` and adapt), `package.json` (`lint:annexure`), `.github/workflows/ci.yml` (restore the `lint-annexure` job from origin/main), `evals/askai/cases/**`, `evals/askai/calibration/owner-grades.json`, `evals/askai/README.md` if it names retired rows.

- [ ] Recreate the annexure with only live rows: the nine FAQ rows (`sandbox-faq-general`, `sandbox-faq-m1`, `sandbox-faq-m2`, `sandbox-faq-m3`, `abdm-faq-sandbox`, `abdm-faq-abha-number`, `abdm-faq-hpr`, `abdm-faq-hfr`, `abdm-faq-general`) copied from `git show origin/main:catalogue/annexure/askai-sources.md`; `glossary` re-pointed at the current glossary source file with its current hash; and one row per file in `catalogue/openapi/.raw/nha-2026-09-16/MANIFEST.md`, id `final-<slug of path>`, hash the committed sha256 truncated as the old rows were. No row for `phr-v3-documents`, `m1-simplified-flow`, `m1-abha-collection` (old path), `spec-errors-m1`, `spec-errors-m2`, `spec-errors-m3`.
- [ ] Delete every case whose `source_row` names a row that no longer exists (expected 25: 18 spec-errors, 5 phr-v3-documents, 2 m1-abha-collection). Remove those case ids from `calibration/owner-grades.json`.
- [ ] In every remaining case, drop `expected_sources` entries naming an atom id that `node -e "import('./scripts/lib/atoms.mjs').then(m=>...)"` does not load. A case left with an empty `expected_sources` keeps it empty.
- [ ] For `glossary` cases: keep a case only if every one of its original `expected_sources` atoms still exists; otherwise delete it and remove it from calibration. Report the count kept and deleted.
- [ ] `npm run -s lint:annexure` passes (it counts citations and fails on a dangling row); `cd mcp && go run ./cmd/askai-eval check -cases ../evals/askai/cases` passes LoadCases (the "no run recorded" message is fine); `go test ./internal/eval/...`.
- [ ] Commit `fix: the Ask AI eval corpus cites sources that exist`.

### Task 4: Committed skill files do not change with the environment

**Files:** `scripts/build-skills.mjs`, `.gitignore`, `site/static/agent-setup/prompt.md` (untracked by this task).

- [ ] Files committed to the repository (`plugins/abdm-integrators-assistant/skills/**`) are rendered without `DOCUSAURUS_URL` and without today's date: the snapshot header names the catalogue version and uses the date encoded in `catalogue/VERSION` (`2026.09.16` becomes `2026-09-16`). `site/static/skills/**` (gitignored) keeps the environment's absolute URLs.
- [ ] `site/static/agent-setup/prompt.md` is generated site output: `git rm --cached` it and add `site/static/agent-setup/prompt.md` to `.gitignore` beside the existing `site/static/skills` entry. Confirm nothing in `site/src` imports it at build time from git (it is fetched at runtime from the built site).
- [ ] Prove: `node scripts/build-skills.mjs` then `DOCUSAURUS_URL=https://example.test node scripts/build-skills.mjs` then `git status --short` shows no tracked change; run it again the next day would change nothing (state the reasoning: no clock input).
- [ ] Commit `fix: committed skills and the setup prompt no longer change with the build environment`.

### Task 5: The site builds with broken links set to throw

**Files:** hand-written pages under `site/docs/` as listed, `site/docusaurus.config.ts`, `docs/superpowers/plans/2026-09-16-broken-links-for-content-session.md`, new `site/docs/whats-new/2026-09-16.mdx`.

Read `plugins/abdm-contributors-assistant/skills/changelog/SKILL.md`, `nha-voice/SKILL.md` and `page-authoring/SKILL.md` first.

- [ ] Run `DOCUSAURUS_ON_BROKEN_LINKS=throw DOCUSAURUS_URL=https://abdm-docs.dev.eka.care npm run -s build > /tmp/build.log 2>&1` after Tasks 1 and 4, and fix every broken link and broken anchor it reports. Link fixes point at the page that now holds the thing (the generated journey step or operation page, the module index `/docs/hiecm/v3/api/<module>`, or the module errors page if it exists); where no page holds it, remove the link and any sentence that promises the thing.
- [ ] Testing pages: delete `site/docs/hiecm/v3/resources/testing/` (index and `_category_.json`), and remove every link and sentence pointing readers to test cases or testing pages from `milestones/m1..m4.mdx`, `resources/index.*` and any other page. No replacement promise.
- [ ] Pages that pointed at `api/m1/apis` or `api/m4/undocumented`: re-point to the module index, and delete any sentence saying operations are unpublished, undocumented, or carried only by a collection.
- [ ] Residue sentences: `site/docs/hiecm/v3/api/README.md` loses its `x-abdm-requirement` and `build-requirements` text; `milestones/m1.mdx` loses the sentence attributing a table to NHA's proposed simplified flow.
- [ ] What's New: delete the six entries `2026-08-24` to `2026-09-10`; every one describes an ingest or a feature built on a retired source. Write `2026-09-16.mdx` per the changelog skill: the reference now rests on NHA's final specification set of 16 September 2026; modules gateway, M1 to M4, P1 to P4, subscriptions, Scan and Pay; each ABHA creation and login method is its own sequence in the reference; error pages list the codes the specifications return. Update `whats-new/index.mdx` if it lists entries.
- [ ] `site/docusaurus.config.ts`: `onBrokenLinks` back to `'throw'` with no environment override.
- [ ] Prove: `DOCUSAURUS_URL=https://abdm-docs.dev.eka.care npm run -s build` exits 0; then `node scripts/build-skills.mjs` and `git status --short` shows only intended changes; `npm run -s build:routes && npm run -s check:routes`; `npm run -s lint:tables`; `npm run -s lint:content`.
- [ ] Rewrite the handoff file to a short record: what was broken, what this task changed, and that nothing remains open for the content session from the reset. Commit in two: `fix: every link and anchor resolves again after the reset` (pages, config) and `docs: what's new describes the final set, and the retired-source entries come down` (whats-new, handoff with `git add -f`).

### Task 6: The contributor plan describes the repository as it is

**Files:** `plan/abdm-v1-phase1-architecture-and-plan.md`, `plan/plan-history/`, `plan/manifest.json`, `plan/gantt/build_gantt.py` (stamp only), the four plan-compiled skills in `plugins/abdm-contributors-assistant/skills/` (`portal-architecture`, `portal-planning`, `dpg-governance`, `abdm-portal-index`), and any other contributor-plugin file stating a count or module that is no longer true.

- [ ] Copy the current plan to `plan/plan-history/plan-2026.09.09.md` (the folder's naming is `plan-<version>.md`).
- [ ] In the plan, rewrite only statements of fact the reset made false: the atom counts paragraph in `p7-coverage` (current numbers from `npm run -s lint:atoms`), module and spec file names (`hiecm-p1`..`p3`, `phr-services` become the eleven current specs), how the API reference is ordered (journey files, not flow atoms), where error codes come from (response examples), and how skills compile (journeys and specs). Principles, dates, owners and done criteria are untouched, so `breaking` is `false`.
- [ ] Bump `plan/manifest.json` `plan_version` to `2026.09.17`, `plan_hash` to the new sha256, and restamp `plan_version`/`plan_hash` in the four compiled skills and `PLAN_VERSION` in `build_gantt.py`. In `abdm-portal-index/SKILL.md` rewrite the "Which gateway, which phase" paragraph and table from the same facts.
- [ ] `grep -rn -i "319\|hiecm-p[123]\|phr-services\|flow atom\|x-abdm-use-case\|x-abdm-errors\|test matri\|Aarogya Setu collection" plugins/abdm-contributors-assistant plan/abdm-v1-phase1-architecture-and-plan.md` returns nothing that states a current fact (historical mentions inside `plan-history/` are expected).
- [ ] `./scripts/plan-check.sh` exits 0; `npm run -s check:plugins`.
- [ ] Commit `docs: the plan and its compiled skills describe the catalogue after the reset`.

### Task 7: Final review

- [ ] Dispatch `abdm-contributors-assistant:adversarial-reviewer` over `8e40ab4a5..HEAD` against this plan and the reset spec, with the same eight attack areas as the reset's final review plus: the MCP index built from the branch answers `decode_error` for a code in the M1 spec; `onBrokenLinks: 'throw'` build passes; no tracked file changes after a build with `DOCUSAURUS_URL`.
- [ ] One fix wave, one scoped re-review, push.
