# P4 API reference

The P4 reference is an interactive page. It renders in a browser and
carries no text to read here.

The same operations in a form you can read:

- [Every P4 page as markdown](/docs/hiecm/v3/api/p4), one page per operation, each with its headers, parameters, responses and a worked curl.
- [The OpenAPI specification](/specs/hiecm-p4.yaml) this reference is generated from.
- [The module index](/docs/hiecm/v3/api/p4/llms.txt), which lists every operation with a link.
